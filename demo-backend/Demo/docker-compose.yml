version: '3.4'

services:
  authorize.api:
    image: ${DOCKER_REGISTRY}/authorizeapi
    build:
      context: .
      dockerfile: Authorize/Authorize/Dockerfile

  account.api:
    image: ${DOCKER_REGISTRY}/accountapi
    build:
      context: .
      dockerfile: Services/Account/Account.API/Dockerfile
    
  minio:
    image: minio/minio

  pg-database:
    image: postgres:11

  kong-migrations:
    image: kong:3.4

  kong-node-1:
    image: kong:3.4
    
  presence:
    image: ${DOCKER_REGISTRY}/presence
    build:
      context: .
      dockerfile: Services/Presence/Presence/Dockerfile
  
#  zookeeper:
#    image: wurstmeister/zookeeper:3.4.6

  channel.api:
    image: ${DOCKER_REGISTRY}/channelapi
    build:
      context: .
      dockerfile: Services/Channel/Channel.API/Dockerfile

  redis:
    image: redis:7

#
#  directmessage.api:
#    image: ${DOCKER_REGISTRY-}/directmessageapi
#    build:
#      context: .
#      dockerfile: Services/DirectMessage/DirectMessage.API/Dockerfile


  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.9.2
    
  kibana:
    image: docker.elastic.co/kibana/kibana:7.9.2

